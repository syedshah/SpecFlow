namespace DemoExampleSite.specs
{
    public static class TestData
    {
        public static void CreateUser(string name)
        {
            // Here we would create a user in the system
            // if it didn't already exist.

            // This could be by:
            //  - direct insert into the user db table
            //  - via the app service layer
            //  - or alternatively through UI
            
            // For demo purposes, the user MrAwesome is
            // hard-coded in the demo example site.
        }
    }
}